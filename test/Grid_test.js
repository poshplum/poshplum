describe('Grid', function () {
  it("has grid style")
  it("gives caller a simple width= prop for shorthand grid-template-columns")
  it("gives caller a simple gap= prop for shorthand grid-gap")
  it("gives caller a simple gap= prop for shorthand grid-gap")
  it("gives caller a simple align= prop for shorthand align-items")
  it("lets caller specify any other props")
  it("lets caller specify the tag to use instead of <div>")
  describe("<Grid.Item>", () => {
    it("gives caller a simple span= prop for grid-column: span ...")
  })
});